import React from "react";
import "./blueCircle.scss";

const BlueCircle = ({
  className
}) => {
  return <div className = {
    `blue-circle ${className}`
  } > < /div>;
};

export default BlueCircle;